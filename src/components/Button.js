import styled from "styled-components"

const Button = styled.button`
    border: 0;
    border-radius: 32px;
    height: 64px;

    background: #e19db4;
    padding: 20px;
    font-weight: 700;
    color: #fff;
`

export default Button;